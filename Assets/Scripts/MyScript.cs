using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MyScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        float maxTime = 5.6f;
        Debug.Log("val" + maxTime);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
